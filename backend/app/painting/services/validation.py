"""Code-convention, inventory, and color-accuracy validation.

Stub — built in M1/M2 (spec §8.4). Every paint reference, including inside
JSON blocks, must resolve to an owned paint with a pattern-valid code.
"""
