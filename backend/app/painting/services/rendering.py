"""Static-HTML export: serialize a structured guide back to the legacy
self-contained HTML file format (spec §9.5).

Stub — built in M2; doubles as the renderer's acceptance test via the importer.
"""
